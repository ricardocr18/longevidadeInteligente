# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI — LLM (gpt-4o), STT (Whisper) e TTS
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ElevenLabs (TTS principal)
ELEVENLABS_API_KEY=...
ELEVENLABS_VOICE_ID=...

# Aplicação
APP_ENV=development          # development | production
LOG_LEVEL=INFO
DATABASE_URL=vivia.db        # caminho do SQLite

# Railway (preenchido automaticamente no deploy)
PORT=8000